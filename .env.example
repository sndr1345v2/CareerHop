# Development/Production mode
NODE_ENV=development

# Database connection string (only needed for PostgreSQL mode)
# For local development with PostgreSQL:
# DATABASE_URL=postgres://username:password@localhost:5432/careerhop
# 
# For Google Cloud SQL with Unix socket:
# DATABASE_URL=postgres://username:password@localhost/careerhop
# INSTANCE_CONNECTION_NAME=project:region:instance

# Session encryption key (required)
SESSION_SECRET=your-secret-key-here

# Port (defaults to 5000 in Replit or 8080 in Google Cloud if not specified)
# PORT=8080